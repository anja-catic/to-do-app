services:
  - type: web
    name: to-do-backend
    env: python
    region: frankfurt
    plan: free
    buildCommand: ""
    startCommand: "./start.sh"
    envVars:
      - key: DATABASE_URL
        value: sqlite:///todo.db
